// the header has the page name (id) and level (class) and first className
const header = document.querySelector("header[id]");
const head = document.head;

// loading -----------------------
const categoryLoad = ()=> {
  load();
}


// loading end -----------------------